Placez ici le binaire `cloudflared` embarqué dans la release.

- Windows : `tunnel/cloudflared.exe`
- Linux/macOS : `tunnel/cloudflared`

Le fichier `vigile.spec` échouera volontairement si le binaire est absent.
